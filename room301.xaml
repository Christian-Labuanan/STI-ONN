<Window x:Class="STI_ONN.room301"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:STI_ONN"
        mc:Ignorable="d"
        Title="room301" Height="465" Width="809"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid Background="#FFEAF2FF">
        <DataGrid x:Name="excelDataGrid" Margin="45,49,45,53" HeadersVisibility="None" AutoGenerateColumns="False" Width="719" IsReadOnly="True">
            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Setter Property="Background" Value="#FFF2CC"/>
                    <Setter Property="Foreground" Value="Black"/>
                    <!-- Default text color -->
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=Column.DisplayIndex}" Value="0">
                            <Setter Property="Background" Value="#305496"/>
                            <Setter Property="Foreground" Value="White"/>
                            <!-- Change text color to white for cells in the first column -->
                        </DataTrigger>
                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=Row.DisplayIndex}" Value="0">
                            <Setter Property="Background" Value="#305496"/>
                            <Setter Property="Foreground" Value="White"/>
                            <!-- Change text color to white for cells in the first row -->
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.CellStyle>
            <DataGrid.Resources>
                <Style x:Key="CenteredText" TargetType="TextBlock">
                    <Setter Property="HorizontalAlignment" Value="Center"/>
                    <Setter Property="VerticalAlignment" Value="Center"/>
                </Style>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Column1" Binding="{Binding Column1}" Width="100" ElementStyle="{StaticResource CenteredText}" />
                <DataGridTextColumn Header="Column2" Binding="{Binding Column2}" Width="100" ElementStyle="{StaticResource CenteredText}" />
                <DataGridTextColumn Header="Column3" Binding="{Binding Column3}" Width="100" ElementStyle="{StaticResource CenteredText}" />
                <DataGridTextColumn Header="Column4" Binding="{Binding Column4}" Width="100" ElementStyle="{StaticResource CenteredText}" />
                <DataGridTextColumn Header="Column5" Binding="{Binding Column5}" Width="100" ElementStyle="{StaticResource CenteredText}" />
                <DataGridTextColumn Header="Column6" Binding="{Binding Column6}" Width="100" ElementStyle="{StaticResource CenteredText}" />
                <DataGridTextColumn Header="Column7" Binding="{Binding Column7}" Width="100" ElementStyle="{StaticResource CenteredText}" />
            </DataGrid.Columns>
        </DataGrid>
        <Button Content="Close" Click="CloseButton_Click" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,417,19,0" Height="44" Width="72" FontSize="18" FontFamily="Arial" FontWeight="Bold" BorderBrush="{x:Null}" Background="#FF305496" Foreground="White">
            <Button.Resources>
                <Style TargetType="{x:Type Border}">
                    <Setter Property="CornerRadius" Value="20"/>
                </Style>
            </Button.Resources>
        </Button>
        <Label x:Name="roomLabel" Content="Room Schedules" HorizontalAlignment="Center" Margin="0,10,0,0" VerticalAlignment="Top" FontSize="20" FontWeight="Bold" FontFamily="Arial"/>
    </Grid>
</Window>
